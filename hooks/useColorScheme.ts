// Este archivo simplemente exporta el hook useColorScheme desde react-native. Este hook devuelve el esquema de color actual (light o dark) del sistema operativo.
export { useColorScheme } from 'react-native';
